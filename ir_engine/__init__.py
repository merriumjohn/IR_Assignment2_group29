"""
IR Engine package - supporting modules for Assignment 2 Streamlit application.

Modules:
    crawler       - configurable-depth web crawler with dedup + metadata separation
    preprocessing - text cleaning, tokenization, keyword extraction, doc profiling
    indexing      - inverted index construction & management
    ranking       - vector space search, PageRank, HITS
    recommender   - content-based, collaborative and hybrid recommenders
    evaluation    - IR effectiveness metrics (P, R, F1, P@K, R@K, MAP, MRR, NDCG)
"""
